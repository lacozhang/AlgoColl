/*一个数组是由一个递减数列左移若干位形成的，比如{4，3，2，1，6，5}

是由{6，5，4，3，2，1}左移两位形成的，在这种数组中查找某一个数。*/

#include <iostream>

using namespace std;

int shift_search( int data, int val ){
		int low, high, mid;
}

int main( int argc, char* argv[] ){
		
		cout << shift_search( data, val ) << "\n";
		return 0;
}
